repositories {
    mavenCentral()
    maven {
        mavenContent {
            snapshotsOnly()
        }
    }
}

tasks.withType(Test) {
    testLogging {
        showStandardStreams = true
        exceptionFormat = 'full'
    }
}
