{{>common/licenseInfo}}
package {{invokerPackage}}.auth.config

import io.micronaut.context.annotation.ConfigurationInject
import io.micronaut.context.annotation.EachProperty
import io.micronaut.context.annotation.Parameter
import io.micronaut.core.annotation.NonNull
import io.micronaut.http.MutableHttpRequest
{{#generatedAnnotation}}
import {{javaxPackage}}.annotation.Generated
{{/generatedAnnotation}}

{{#generatedAnnotation}}
{{>common/generatedAnnotation}}
{{/generatedAnnotation}}
@EachProperty("security{{#authConfigName}}.{{{authConfigName}}}{{/authConfigName}}.basic-auth")
{{#nonPublicApi}}internal {{/nonPublicApi}}{{^nonPublicApi}}{{#explicitApi}}public {{/explicitApi}}{{/nonPublicApi}}open class HttpBasicAuthConfig @ConfigurationInject constructor(
    @Parameter
    override val name: String,
    @NonNull
    var username: String,
    @NonNull
    var password: String,
) : ConfigurableAuthorization {

    override fun applyAuthorization(@NonNull request: MutableHttpRequest<*>) {
        request.basicAuth(username, password)
    }
}
