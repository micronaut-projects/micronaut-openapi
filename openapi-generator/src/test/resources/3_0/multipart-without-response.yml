openapi: 3.1.0
info:
  description: API
  title: API
  version: 1.0.0
paths:
  /test:
    put:
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
