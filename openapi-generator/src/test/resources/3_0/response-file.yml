openapi: 3.1.0
info:
  version: "1"
  title: api
paths:
  /example-route:
    get:
      responses:
        "200":
          description: get example
          required: true
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
