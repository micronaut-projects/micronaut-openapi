plugins {
    id "io.micronaut.build.internal.docs"
    id "io.micronaut.build.internal.quality-reporting"
}

allprojects {
    configurations.all {
        all*.exclude group: "io.spring.nohttp"
    }
}
