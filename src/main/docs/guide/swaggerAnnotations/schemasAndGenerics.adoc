If a method return type includes generics then these will be included when calculating the schema name. For example the following:

.Swagger returns types and generics
snippet::io.micronaut.configuration.openapi.docs.generics.Response,io.micronaut.configuration.openapi.docs.generics.MyController[tags="clazz", project-base="docs-examples/example", source="main"]

Will result in a schema called `#/components/schemas/Response_Pet_` being generated. If you wish to alter the name of the schema you can do so with the `@Schema` annotation:

.Changing the name of response schema
snippet::io.micronaut.configuration.openapi.docs.generics.NamedResponseController[tags="method", indent=0, project-base="docs-examples/example", source="main"]

In the above case the generated schema will be named `#/components/schemas/ResponseOfPet`.
