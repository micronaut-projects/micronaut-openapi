Views support https://github.com/scalar/scalar[Scalar], to enable it use `scalar.enabled=true`.

The views will be generated to the `META-INF/swagger/views/scalar` directory of your project’s class output.

|===
| `scalar.enabled` | `true` or `false`  When 'true' the scalar view is generated.
| `scalar.js.url` | Override path to use to find the js file. Path must contain a file named scalar.standalone.js. The value can be a URL path like: "https://cdn.jsdelivr.net/npm/scalar/bundles/". Must contain trailing slash.
| `scalar.spec.url` | Override path in swagger file path line. If you don't set, then value will be `<context-path>/swagger/<spec_filename>`. You can use the placeholder `${filename}` it will be replaced with the final name of the generated swagger file.
| `scalar.copy-resources` | Copy or not local JS and CSS resources. Set to `false` if you are using external resources (e.g. https://cdn.jsdelivr.net/npm/scalar/bundles/). Default `true`
| `scalar.style` | Custom CSS style description for scalar. Inserted into the block: `<style>${scalar.style}</style>`
| `scalar.customDomain` |
| `scalar.theme` |
| `scalar.isEditable` |
| `scalar.spec` |
| `scalar.proxyUrl` |
| `scalar.showSidebar` |
| `scalar.hideModels` |
| `scalar.hideDownloadButton` |
| `scalar.hideTestRequestButton` |
| `scalar.hideSearch` |
| `scalar.darkMode` |
| `scalar.forceDarkModeState` |
| `scalar.hideDarkModeToggle` |
| `scalar.customCss` |
| `scalar.searchHotKey` |
| `scalar.baseServerURL` |
| `scalar.servers` |
| `scalar.metadata` |
| `scalar.favicon` |
| `scalar.defaultHttpClient` |
| `scalar.hiddenClients` |
| `scalar.onSpecUpdate` |
| `scalar.authentication` |
| `scalar.generateHeadingSlug` |
| `scalar.generateModelSlug` |
| `scalar.generateTagSlug` |
| `scalar.generateOperationSlug` |
| `scalar.generateWebhookSlug` |
| `scalar.onLoaded` |
| `scalar.withDefaultFonts` |
| `scalar.defaultOpenAllTags` |
| `scalar.tagsSorter` |
| `scalar.operationsSorter` |
| `scalar.hideClientButton` |

|===

See https://github.com/scalar/scalar/blob/main/documentation/configuration.md[Scalar config] for a description of the above properties.

To expose the `scalar` views, you also must expose the generated `yaml`:

.Exposing Swagger YAML And Scalar Views
[configuration]
----
micronaut:
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
      scalar:
        paths: classpath:META-INF/swagger/views/scalar
        mapping: /scalar/**
----

With the above configuration in place when you run your application you can access your Swagger documentation at +http://localhost:8080/scalar+.
